---
id: getting-started
title: Getting Started
sidebar_position: 1
description: Generate a FlixelGDX game project in a single click — pick a language, platforms, IDE and template.
---

import ProjectGenerator from '@site/src/components/projectGenerator/ProjectGenerator';

# Getting Started

Welcome to **FlixelGDX**! This page is the fastest way to go from
"I want to make a game" to a working project on your machine. Fill in the
form below, click **Download project**, and you will get a zip of a
ready-to-run Gradle project — with the FlixelGDX framework wired in via
[JitPack](https://jitpack.io/#flixelgdx/flixelgdx), a game class, a play
state, and IDE files matching whatever editor you use.

:::tip[Hover any ? for help]

Every option has a friendly description that pops up on hover. The big
**Expert mode** toggle at the bottom reveals advanced knobs (heap size,
extra JVM flags, free-form Gradle config) — leave it off if you are just
getting started.

:::

## Project generator

<ProjectGenerator />

## After you download

1. **Install a JDK** — Java 17 or newer. The generated `README.md`
   has step-by-step install instructions for the JDK and operating
   system you picked above. The [Pong tutorial's Step 0](./your-first-project#0-install-a-jdk)
   has the same walk-through embedded if you'd rather scroll than
   open a separate file.
2. **Unzip** the downloaded archive anywhere on your computer.
3. From a terminal in that folder, run your game on the desktop:

   ```bash
   ./gradlew :lwjgl3:run        # macOS / Linux
   gradlew.bat :lwjgl3:run      # Windows
   ```

   The zip already ships the Gradle wrapper, and the build script
   tells Gradle's [Foojay Toolchains Resolver](https://github.com/gradle/foojay-toolchains)
   to auto-download the matching JDK on first run — so the very first
   build takes a minute, every build after that is instant.

A red square (or your platformer player) should appear in a 640×480
window. You are running a real FlixelGDX game. 🎉

:::tip[JitPack vs Gradle plugin IDs]

The framework JARs resolve as `com.github.flixelgdx.flixelgdx:…` from JitPack,
but the published **Gradle plugins** keep their `me.stringdotjar.flixelgdx.*`
IDs. Generated `settings.gradle` wires those IDs to the right JitPack
artifacts automatically; if you hand-edit Gradle files, mirror the snippet in the
framework’s [COMPILING.md](https://github.com/flixelgdx/flixelgdx/blob/master/COMPILING.md)
so plugins keep resolving.

:::

## Where to go next

- Head to **[Your First Project](./your-first-project)** to build the
  classic Pong from scratch, step by step.
- Browse the **[API reference](/api/)** when you want to look up a class.
- Drop into the framework repo at
  [`flixelgdx/flixelgdx`](https://github.com/flixelgdx/flixelgdx) to file
  issues, read source, or grab a snapshot build.
