/*
 * Root build script — intentionally empty.
 *
 * All real work happens in the `dokka/` subproject, which renders the
 * FlixelGDX API reference as GitHub-Flavoured Markdown for Docusaurus to
 * consume. From the repo root, you can run:
 *
 *   ./gradlew :dokka:dokkaGfm
 *
 * or just use the higher-level wrapper:
 *
 *   ./scripts/build-api.sh
 */
